import { SeerBitButtonDirective } from './seer-bit.directive';

describe('SeerBitButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SeerBitButtonDirective();
    expect(directive).toBeTruthy();
  });
});
